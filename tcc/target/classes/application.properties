# ------------------------------------
# CONFIGURA��ES DE CONEX�O MYSQL (Comentadas)
# ------------------------------------
# spring.datasource.url=jdbc:mysql://localhost:3306/tcc?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=
# spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver

# ------------------------------------
# CONFIGURA��ES DE CONEX�O H2 DATABASE (Adicionadas)
# ------------------------------------
# Usar o driver do H2
spring.datasource.driverClassName=org.h2.Driver

# URL de conex�o: Cria/usa um ficheiro chamado 'oficina_db' na raiz do projeto
spring.datasource.url=jdbc:h2:file:./oficina_db

# Utilizador e senha padr�o para H2 (n�o precisa de mudar)
spring.datasource.username=sa
spring.datasource.password=

# Habilita a consola H2, acess�vel em http://localhost:8080/h2-console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ------------------------------------
# CONFIGURA��ES JPA/HIBERNATE (Mantidas e Ajustadas)
# ------------------------------------
# Comentar ou remover a plataforma MySQL
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# 'update': Cria/atualiza tabelas automaticamente. Bom para desenvolvimento.
spring.jpa.hibernate.ddl-auto=update

# Mostra os comandos SQL executados no console (�til para debug)
spring.jpa.show-sql=true

# O dialeto H2 geralmente � detectado automaticamente, mas pode ser especificado se necess�rio
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# Outras configura��es (como a Geocoding API do Google, se tiver)
# ...